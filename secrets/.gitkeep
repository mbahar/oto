# API keys and tokens stored here — git-ignored, chmod 600
# Format: platform.env (KEY=VALUE per line)
# Example:
#   lifx.env     → LIFX_TOKEN=xxx
#   twilio.env   → ACCOUNT_SID=xxx / AUTH_TOKEN=xxx / FROM_NUMBER=xxx
#   arlo.env     → ARLO_EMAIL=xxx / ARLO_PASSWORD=xxx
